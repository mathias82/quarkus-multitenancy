---
artifact: ${project.groupId}:${project.artifactId}:${project.version}
name: "Multitenancy - HTTP"
description: "HTTP transport (header, cookie, JWT, or path) for tenant resolution."
metadata:
  keywords:
    - "multitenancy"
    - "multi-tenant"
    - "tenant"
    - "http"
  guide: "https://docs.quarkiverse.io/quarkus-multitenancy/dev/"
  categories:
    - "core"
    - "web"
  status: "preview"
  config:
    - "quarkus.multi-tenant.http."
